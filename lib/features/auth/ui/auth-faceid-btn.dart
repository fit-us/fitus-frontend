import 'package:flutter/material.dart';

class AuthFaceIdButton extends StatelessWidget {
  const AuthFaceIdButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
